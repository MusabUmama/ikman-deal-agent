# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
websockets==13.1
# Data validation — the backbone of everything
pydantic==2.9.2
pydantic-settings==2.5.2
# LLM providers (all free tier)
groq==0.11.0
google-generativeai==0.8.2
# Async HTTP — never use requests for an async service
httpx==0.27.2
# HTML parsing
beautifulsoup4==4.12.3
lxml==5.3.0
# Browser automation (phone reveal + WhatsApp Web)
playwright==1.47.0
# Database — fully async
aiosqlite==0.20.0
sqlalchemy[asyncio]==2.0.35
# Structured logging
structlog==24.4.0
# Utilities
python-dotenv==1.0.1
tenacity==9.0.0
